# Tasks - Get all

## Get all tasks

* I send a GET request to "/tasks" for get all tasks
* the response status code should be "200"
* the response should be a non-empty array
* the response should contain task with id "created_task_id"
